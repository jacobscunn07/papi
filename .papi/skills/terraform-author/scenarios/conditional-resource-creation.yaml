id: conditional-resource-creation
prompt: |
  Write a Terraform module for an S3 bucket where both the bucket itself and
  an optional bucket policy can each be independently enabled or disabled at
  call time. Show variables.tf and main.tf.
tags: [create-variable, conditional]
