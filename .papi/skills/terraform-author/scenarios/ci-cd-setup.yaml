id: ci-cd-setup
prompt: |
  Set up automated validation for our Terraform repositories. We use GitHub
  Actions and want to catch issues before they reach production.
weight: 1.0
tags: [ci-cd]
fixtures:
  main.tf: |
    resource "aws_s3_bucket" "example" {
      bucket = "my-bucket-${var.env}"
    }
    variable "env" {
      type = string
    }
